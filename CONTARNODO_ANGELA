 int contarNodos(Node* nodo) {
     if (nodo == NULL)
         return 0;
     else
         return 1 + contarNodos(nodo->left) + contarNodos(nodo->right);
 }

//int numNodos = contarNodos(arbol);
      

